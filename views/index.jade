doctype html
//if lt IE 7
  html.no-js.lt-ie9.lt-ie8.lt-ie7  
//if IE 7
  html.no-js.lt-ie9.lt-ie8  
//if IE 8
  html.no-js.lt-ie9  
// [if gt IE 8] <!
html.no-js
    // <![endif]
    head
        meta(charset='utf-8')
        meta(http-equiv='X-UA-Compatible', content='IE=edge')
        title
        meta(name='description', content='')
        meta(name='viewport', content='width=device-width, initial-scale=1')
        // Place favicon.ico and apple-touch-icon.png in the root directory
    body
        script(src='/socket.io/socket.io.js')
        script.
            io = io.connect()
            // Emit ready event.
            io.emit('ready')
            //console.log('emit ready')
            io.on('newphoto', function(data) {
                console.log("photoid: " + data.photoid + " camid: " + data.camid)
                document.getElementById("cam"+data.camid).src="/photo/"+data.photoid;
            });
            io.on('new message', function(data) {
                console.log('broadcast');
            });
            io.on('announce', function(data) {
                console.log("New message in announce: " + data.message);
            })
        //if lt IE 7
          p.browsehappy
            | You are using an
            strong outdated
            |  browser. Please
            a(href='http://browsehappy.com/') upgrade your browser
            |  to improve your experience.
        // Add your site or application content here
        h2
        img#camxx(width='300')
        img#cam1(width='300')
        p#textdata
